Delegates and Events
 - Do them better.
 - Resync the C++0x version to the teplate version.

Filesystem
 - Delete Recursive

IO
 - Text{Reader,Writer}
 - Network
 - Console, it was hacked together *really* badly.

Threading
 - Create a ThreadLocalStorage pointer class.

Misc
 - Make everything public virtual inheritance
 - Fix autoalloc, it's broken if the first base class isn't Object
